#pragma once
#include <stdlib.h>
#include <vector>
#include <string>

using namespace std;
class Menu
{
private:
	~Menu();
public:
	Menu();
	void printMenu(); //to print the menu options stored in vOptions

};

